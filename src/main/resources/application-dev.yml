# 개발설정
spring:
  # DB접속정보
  datasource:
    url: jdbc:postgresql://localhost:15432/nexus_prod
    username: nexus
    password: nexus6993@@
    driver-class-name: org.postgresql.Driver
  # 레디스
  data:
    redis:
      host: localhost
      port: 6379
    
server:
  port: 4000

app:
  short-url:
    base-url: http://localhost:4000/re/

# 로깅 설정 (개발용 상세 로그)
logging:
  level:
    com.apr.backend: DEBUG                    # 우리 프로젝트 패키지 DEBUG 레벨
    org.springframework.web: DEBUG           # Spring Web DEBUG 레벨
    org.springframework.boot.autoconfigure: ERROR
    org.hibernate.SQL: DEBUG                 # Hibernate SQL 쿼리 로그
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE  # SQL 파라미터 바인딩 로그
    org.hibernate.orm.jdbc.bind: trace